# --- ROS 2 / colcon workspace (never commit build products) ---
build/
install/
log/

# --- Python (launch files, scripts, test tools) ---
__pycache__/
*.py[cod]
*$py.class
.Python
*.so
.venv/
venv/
env/

# --- C++ / CMake (in-package or stray in-source builds) ---
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile
compile_commands.json
CTestTestfile.cmake
*.a

# --- Editors & OS ---
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Optional: uncomment if you keep personal editor config local only
# .vscode/

# --- Large / machine-local artifacts (uncomment if you use them in-tree) ---
# *.bag
# *.mcap

debug_logs/

# FAST-LIO / livox: clone via vcs (see bot_mapping/repos/fast_lio.repos)
src/FAST_LIO/

# Saved 3D maps (local; copy from /tmp after map_save)
maps/*.pcd
maps/*.ply
